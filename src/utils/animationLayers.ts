/**
 * Animasi disederhanakan — Framer Motion untuk UI saja (modal, sidebar, hover).
 * GSAP scroll reveal DIHAPUS (menyebabkan elemen opacity:0 & tidak muncul).
 * Anime.js hanya di TerminalOutput (admin login) bila perlu.
 */

export const ANIM_LAYER = {
  framer: 'framer',
} as const;
